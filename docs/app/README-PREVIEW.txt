A preview of app.iptmusic.com, so the owner can look at what is about to ship without waiting on a
deployment. Not the product's address: that is app.iptmusic.com, and this copy is replaced whenever
the client changes.

The demo works here completely — it is the whole app with a seeded studio and needs no account.
Signing in does not, and cannot: Supabase's redirect allow-list names the real origins, and adding
a github.io address to it would widen the surface of a live project for a preview.
